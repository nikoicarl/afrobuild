<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
        <%= (!setupData?.name || setupData.name==="" ) ? 'AfroBuild' : setupData.name %> || Portal
    </title>

    <% if (setupData?.logo) { %>
        <link rel="icon" href="uploads/<%= setupData.logo %>" type="image/x-icon" />
        <% } else { %>
            <link rel="icon" href="assets/img/afrobuild.png" type="image/x-icon" />
            <% } %>

                <link href="assets/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet" />
                <style>
                    body,
                    html {
                        height: 100%;
                        margin: 0;
                        background-color: #f8f9fa;
                    }

                    .login-container {
                        min-height: 100vh;
                        display: flex;
                        align-items: center;
                        justify-content: center;
                        padding: 15px;
                    }

                    .login-box {
                        width: 100%;
                        max-width: 400px;
                        padding: 30px;
                        background: #fff;
                        border-radius: 10px;
                        box-shadow: 0 0 15px rgba(0, 0, 0, 0.05);
                        text-align: center;
                    }

                    .logo {
                        max-width: 150px;
                        margin-bottom: 20px;
                    }

                    .form-control:focus {
                        box-shadow: none;
                        border-color: #198754;
                    }

                    @media (max-width: 576px) {
                        .login-box {
                            padding: 20px;
                        }

                        .logo {
                            max-width: 120px;
                        }
                    }
                </style>
</head>

<body>
    <div class="login-container">
        <div class="login-box">
            <% if (setupData?.logo) { %>
                <img src="uploads/<%= setupData.logo %>" alt="Logo" class="logo" />
                <% } %>

                    <h3 class="mb-4">Sign In</h3>
                    <form action="" class="text-start afrobuild-login-submit">
                        <div class="mb-3">
                            <input type="text" name="username" class="form-control username" placeholder="Username"
                                required />
                        </div>
                        <div class="mb-3 position-relative">
                            <input type="password" id="password" name="password" class="form-control password"
                                placeholder="Password" required />
                            <!-- Optional: password visibility toggle icon can be added here -->
                        </div>
                        <button type="submit" class="btn btn-success w-100 afrobuild-login-form-btn">Sign In</button>
                    </form>
        </div>
    </div>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>

    <script src="assets/js/bootstrap.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="/ajax/app/loginAjax.js"></script>
</body>

</html>