
<% String.prototype.fullDate = function() { %>
    <% var inputDate = this; %> 
    <% if (inputDate == "" || inputDate == null || inputDate == undefined) { %>
        <% return ''; %>
    <% } else { %>
        <% var date = new Date(inputDate); %>
        <% return date.toDateString(); %>
    <% } %>
<% } %>

<% String.prototype.toUcwords = function() { %>
    <% let value = this; %>
    <% if (value == "" || value == null || value == undefined) { %>
        <% return ''; %>
    <% } else { %>
        <% return value.replace(/\w+/g, function(a){ %>
            <% return a.charAt(0).toUpperCase() + a.slice(1).toLowerCase(); %>
        <% }); %>
    <% } %>
<% } %>

<% String.prototype.formatNumber = function() { %>
    <% let value = Number(this); %>
    <% return value.toFixed(2).replace(/\d(?=(\d{3})+\.)/g, '$&,'); %>
<% } %>

<% var queryString = 'pub='+pageNavigate.pub+'&med='+pageNavigate.med; %>

<%- include ('links/headlinks') %>

    <%- include ('navs/navbar') %>

    <!--  BEGIN BREADCRUMB  -->
    <%- include ('navs/breadcrumb') %>
    <!--  END BREADCRUMB  -->

    <!--  BEGIN MAIN CONTAINER  -->
    <div class="main-container" style="width: 100%;" id="container">
        <!--  BEGIN SIDEBAR  -->
        <%- include ('navs/sidebar') %>
        <!--  END SIDEBAR  -->
        
        <!--  BEGIN CONTENT PART  -->
        <div id="content" class="main-content">
            <div class="row">
                <div class="col-md-12 logig_progress_bar_div hide">
                    <div class="progress br-10" style="margin: 0 1.5%; height: 8px; background-color: #ffe1e2;">
                        <div class="progress-bar progress-bar-striped progress-bar-animated bg-info logig_progress_bar" role="progressbar" style="width: 0%" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100"></div>
                    </div>
                </div>
                <div class="col-md-12" id="logig_main_content_display"></div>
            </div>
            
            <input type="hidden" class="hidden_user_email" value="<%= employData.email %> ">
            <input type="hidden" class="hidden_user_fullname" value="<%= employData.first_name %> <%= employData.other_name %> <%= employData.last_name %>">
            <input type="hidden" class="hidden_user_type" value="<%= userData.status %> ">
            <input type="hidden" class="hidden_userid" value="<%= userData.userid %>">
            <input type="hidden" class="hidden_delete_for_admin" value="<%= userData.status %>">
            <input type="hidden" class="hidden_base_currency" value="<%= setupData.currency %>">
            <input type="hidden" class="hidden_base_primarycolor" value="<%= setupData.primary_color %>">
            <input type="hidden" class="logig_hidden_employeedata" value="<%= JSON.stringify(employData); %> ">
            <input type="hidden" class="hidden_privilege_data" value="<%= JSON.stringify(privilege); %>">
            <input type="hidden" class="hidden_setup_data" value="<%= JSON.stringify(setupData); %>">
            
            <!--  BEGIN FOOTER  -->
            <%- include ('navs/footer') %>
            <!--  END FOOTER  -->
        </div>
        <!--  END CONTENT PART  -->

    </div>
    <!-- END MAIN CONTAINER -->

<%- include ('links/footlinks') %>